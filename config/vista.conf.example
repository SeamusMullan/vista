# vista Configuration Example
# Copy to ~/.config/vista/vista.conf or use with -c flag

# Primary wallpaper directory
wallpaper_dir = ~/wallpaper

# Additional wallpaper directories (optional)
# You can add up to 10 additional directories
# wallpaper_dir_1 = ~/Pictures/Wallpapers
# wallpaper_dir_2 = /usr/share/backgrounds
# wallpaper_dir_3 = ~/Downloads/wallpapers

# Command to set wallpaper
# Supported: feh, nitrogen, xwallpaper, swaybg, or custom command
feh_command = feh --bg-scale

# For nitrogen:
# feh_command = nitrogen

# For xwallpaper:
# feh_command = xwallpaper

# For swaybg (Wayland):
# feh_command = swaybg

# ============================================================================
# Multi-Monitor Configuration
# ============================================================================
# Enable per-monitor wallpaper (applies same wallpaper to each monitor separately)
# When enabled, vista will set the wallpaper on each monitor individually
# instead of spanning across all monitors
use_per_monitor = false

# Define your monitor outputs (run 'xrandr' to see your monitor names)
# Uncomment and configure for multi-monitor setups
# monitor_1 = DP-1
# monitor_2 = HDMI-1
# monitor_3 = HDMI-2

# ============================================================================
# Color Scheme / Theme Generation
# ============================================================================
# Use pywal to generate color scheme from wallpaper
# This runs 'wal -i "$WALLPAPER" -n' automatically before setting the wallpaper
# The -n flag tells wal not to set the wallpaper (we do it ourselves with feh)
use_wal = false

# Additional options to pass to wal command (optional)
# Useful for customizing color generation behavior
# Examples:
#   --backend colorz       = Use colorz backend (more reliable terminal updates)
#   --backend colorthief   = Use colorthief backend (faster)
#   --saturate 0.7         = Adjust color saturation (0.0 = grayscale, 1.0 = full)
#   -l                     = Generate light colorscheme instead of dark
#   -q                     = Quiet mode (less output)
# wal_options = --backend colorz --saturate 0.7

# Optional: Additional palette/theme script
# This script will be called AFTER wallpaper is set with the wallpaper path as argument
# Can be used alongside or instead of pywal for custom theme generation
# palette_script = ~/.local/bin/theme-update.sh

# ============================================================================
# Window Manager Integration
# ============================================================================
# Automatically reload i3 configuration after wallpaper change
# Useful for applying theme changes to i3 immediately
reload_i3 = false

# Optional: Run additional command after wallpaper change
# The wallpaper path will be passed as an argument
# Useful for restarting polybar, dunst, or other themed applications
# post_command = killall -q polybar; ~/.config/polybar/launch.sh

# ============================================================================
# Display Settings
# ============================================================================
# Thumbnail dimensions
thumbnail_width = 200
thumbnail_height = 150

# Window size
window_width = 1200
window_height = 300

# Enable OpenGL shader rendering (requires building with -DUSE_SHADERS=ON)
use_shaders = false

# Number of thumbnails per row (grid mode)
thumbnails_per_row = 5

# ============================================================================
# Roulette Mode Settings (for -r/--random flag)
# ============================================================================
# Directory containing custom audio files for roulette animation
# If not specified or files not found, procedural sounds will be generated
# Supported formats: .wav, .mp3, .ogg
# Expected files:
#   - tick.wav/mp3/ogg (or roulette_tick.*) - plays when passing wallpapers
#   - select.wav/mp3/ogg (or roulette_select.* or win.*) - plays on final selection
# audio_dir = ~/.config/vista/audio

# Roulette animation timing (in milliseconds)
# These control the speed and duration of the CS:GO-style animation
# Defaults are well-balanced, but you can customize for different feels

# Duration of initial acceleration phase (default: 800)
# Lower = faster start, Higher = slower, more dramatic buildup
roulette_start_duration = 800

# Duration of fast scrolling phase (default: 2000)
# The "blur" phase where items fly by at max speed
# Lower = shorter animation, Higher = longer suspense
roulette_scroll_duration = 2000

# Duration of deceleration phase (default: 2500)
# How long it takes to slow down and land on selection
# Lower = quicker selection, Higher = more suspenseful slowdown
roulette_slow_duration = 2500

# Duration to show final selection (default: 1500)
# How long the selected wallpaper pulses before applying
# Lower = quicker apply, Higher = more celebration time
roulette_show_duration = 1500

# Maximum scroll velocity in items per second (default: 80.0)
# How fast items scroll during the fast phase
# Lower = slower, easier to see items (40-60)
# Higher = faster, more dramatic blur effect (80-120)
roulette_max_velocity = 80.0

